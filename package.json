{
    "private": true,
    "engines": {
        "node": ">=20.9.0"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "start": "npm run start --workspace=web",
        "dev": "npm run dev --workspace=web",
        "build": "npm run build --workspace=web",
        "generate": "npm run generate --workspace=db",
        "db:push": "npm run db:push --workspace=db",
        "postinstall": "npm run generate"
    }
}